# The weekly tool refresh, across the platforms the tools actually run on.
#
# Split the way the work splits: a Linux runner cannot tell you what a tool's
# `--help` says on Windows, so each platform *gathers* — observing releases and
# writing a document of what it saw — and one job *assembles*, folding all three
# into the store. One process holds the pen; three machines committing to
# tool-history/ would be three whole-file conflicts a week.
#
# The release decision stays "were any events appended", never "did a version
# move". A platform looking for the first time widens coverage without being
# news, and the PR opens on any tree change so a week's findings are never
# recomputed next Monday.
#
# An index that would not answer FAILS the run (exit 75): a renamed package
# answers "nothing new" forever, and that must never read as success. A leg
# that dies fails the workflow too — a silently two-platform week would tag
# absences on a platform that simply never reported.
name: refresh

on:
  schedule:
    - cron: "0 6 * * 1" # Mondays 06:00 UTC
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: refresh
  cancel-in-progress: false

jobs:
  gather:
    strategy:
      fail-fast: false # every leg's findings are worth having, even if one dies
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        # bash on every leg, Windows included. A `run:` block there is pwsh
        # by default, and the pre-pass — the job's first multi-line step —
        # is shell script: `$(…)`, `[ … ]`, `>> "$GITHUB_OUTPUT"`. It failed
        # on Windows alone, and took provisioning and the gather with it.
        shell: bash
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
      - run: uv sync

      - name: uv first, so the question can be answered truthfully
        # uv carries CPython's download index inside the binary, so a stale
        # uv reports a stale newest python — and the pre-pass would say
        # "nothing owed" about a release it cannot see. One tool, ~3s.
        env:
          GH_TOKEN: ${{ github.token }}
        run: uv run fm tools.provision --only=uv --strict

      - name: is there anything to read?
        id: owed
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          uv run fm --json tools.owed --prefix=.tools-latest > owed.json
          # `select(.task)` first: the items envelope is one flat list, so a
          # step row (which carries `command`, not `task`) reaches the filter
          # too and `endswith` refuses a null.
          total=$(jq '[.items[] | select(.task) | select(.task | endswith("owed"))
                       | .returned.releases[] | length] | add // 0' owed.json)
          blocked=$(jq '[.items[] | select(.task) | select(.task | endswith("owed"))
                         | .returned.unreachable | length] | add // 0' owed.json)
          echo "total=$total blocked=$blocked"
          # An index that would not answer is not "nothing to do": a walk
          # that cannot see an index cannot say the index has nothing new.
          if [ "$total" = "0" ] && [ "$blocked" = "0" ]; then
            echo "work=false" >> "$GITHUB_OUTPUT"
          else
            echo "work=true" >> "$GITHUB_OUTPUT"
          fi

      - name: provision what a gather actually drives
        # `uv` and `bun`, and nothing else. A gather installs each release
        # itself; the provisioned binaries only drive the tiers — `uv` for
        # the python and PyPI tiers, `bun` for the npm one. Fetching the
        # other 26 was work nothing read, and 26 more chances for a dropped
        # connection to cost a platform its observations. One did: a leg
        # died on gh's zip, for a tool the gather never opens.
        if: steps.owed.outputs.work == 'true'
        env:
          # Provisioning reads release indexes too, and unauthenticated that
          # is 60 calls an hour *per IP* — shared with every other runner in
          # the region. The first dispatch spent it before macOS and Windows
          # got to bun, which failed, and took cspell and markdownlint with
          # it. The token was on the observe step alone.
          GH_TOKEN: ${{ github.token }}
        run: uv run fm tools.provision --only=uv,bun --strict

      - name: observe what this platform sees
        if: steps.owed.outputs.work == 'true'
        env:
          GH_TOKEN: ${{ github.token }} # 5,000/hr; a shared runner's 60 is spent by strangers
        run: >
          uv run fm tools.gather --prefix=.tools-latest
          --out=observations/${{ matrix.os }}.json

      - uses: actions/upload-artifact@v4
        if: steps.owed.outputs.work == 'true'
        with:
          name: observations-${{ matrix.os }}
          path: observations/

  assemble:
    needs: gather
    runs-on: ubuntu-latest
    steps:
      - name: refuse to run without the PAT
        env:
          REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
        run: |
          if [ -z "$REFRESH_TOKEN" ]; then
            echo "::error::secrets.REFRESH_TOKEN is unset. Create a fine-grained PAT" \
                 "(this repo; contents: write, pull-requests: write) and store it as" \
                 "REFRESH_TOKEN — a PR opened with the default GITHUB_TOKEN never" \
                 "triggers CI, so the required gate would sit unrun and auto-merge" \
                 "would wait forever."
            exit 1
          fi

      - uses: actions/checkout@v7
        with:
          token: ${{ secrets.REFRESH_TOKEN }}
      - uses: astral-sh/setup-uv@v7
      - run: uv sync

      - uses: actions/download-artifact@v4
        with:
          path: observations
          pattern: observations-*
          merge-multiple: true

      - name: what reported
        id: reported
        # A leg that *died* never reaches here — `needs: gather` requires
        # every matrix job to succeed, and a failed one skips this job
        # rather than letting its silence read as an answer.
        #
        # A leg that reported nothing is a different thing entirely: the
        # pre-pass found nothing owed, so it skipped provisioning, the walk
        # and the upload. That is the quiet week this workflow was built to
        # cost nothing, and it must not look like a failure.
        run: |
          count=$(ls observations/*.json 2>/dev/null | wc -l | tr -d " ")
          echo "count=$count" >> "$GITHUB_OUTPUT"
          echo "$count platform(s) had something to read"

      - name: fold them into the store
        if: steps.reported.outputs.count != '0'
        run: uv run fm --json tools.assemble observations/*.json > assembled.json

      - name: what changed
        if: steps.reported.outputs.count != '0'
        run: |
          git add -A -- tool-history src/footman/_stubs CHANGELOG.md
          if git diff --cached --quiet; then
            echo "changed=false" >> "$GITHUB_ENV"
          else
            echo "changed=true" >> "$GITHUB_ENV"
          fi
          jq -r '.items[] | select(.task) | select(.task | endswith("assemble"))
                 | .returned | "release=\(.release)"' assembled.json >> "$GITHUB_ENV"

      - name: roll the release
        # Built, and off unless the repo variable says otherwise: a stub-only
        # release is a patch bump, and whether that ships without a human
        # looking is a policy, not a mechanism.
        if: steps.reported.outputs.count != '0' && env.changed == 'true'
          && env.release == 'true' && vars.AUTO_RELEASE == 'true'
        run: |
          uv run fm --json tools.prepare-release > prepared.json
          jq -r '.items[] | select(.task) | select(.task | endswith("prepare-release"))
                 | .returned | "tag=v\(.version)"' prepared.json >> "$GITHUB_ENV"
          git add -A -- pyproject.toml src/footman/__init__.py docs/json.md CHANGELOG.md

      - name: open the pull request
        if: steps.reported.outputs.count != '0' && env.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
        run: |
          branch="chore/tool-refresh-$(date -u +%Y%m%d)"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b "$branch"
          git commit -m "chore(tools): weekly refresh${tag:+ — release $tag}"
          # The branch is named for the day, so a *second* run in one day
          # meets its own leftovers and `git push` is rejected — which is
          # every re-run after a failure or a discarded PR, and it cost one.
          # `--force-with-lease` is the safe spelling: it refuses if someone
          # else moved the branch since we fetched it, so a human's commits
          # on an open refresh PR are never overwritten.
          git fetch origin "$branch" --depth=1 2>/dev/null || true
          git push --force-with-lease origin "$branch"
          # An open PR already points at this head: re-pushing updated it,
          # and a second `gh pr create` would only fail. Ask first — and ask
          # about *open* ones. `gh pr view <branch>` answers for a closed PR
          # too, so discarding a bad refresh and re-running left the branch
          # pushed, the guard satisfied by the PR that had just been closed,
          # and no PR opened for the work.
          if [ -n "$(gh pr list --head "$branch" --state open --json number \
                     --jq '.[].number')" ]; then
            echo "refreshed the open PR for $branch"
            exit 0
          fi
          gh pr create --base main --head "$branch" \
            --title "chore(tools): weekly tool refresh${tag:+ ($tag)}" \
            --body "$(printf 'Observed on ubuntu, macOS and Windows; folded by one assembler.\n\n```\n%s\n```\n\nAuto-merges once the gate is green — the gate is the reviewer here: it replays every chain, regenerates every stub, and runs the drift tests.' \
              "$(jq -r '.items[] | select(.task) | select(.task | endswith("assemble")) | .output' assembled.json)")"
          # Opened, not merged. The gate replays every chain and
          # regenerates every stub, which proves the store is *consistent* —
          # not that the readings in it are true. A wrong reading passes it
          # unchanged: the first dispatch of this workflow read `build`
          # with six flags on one option and would have merged that.

      - name: tag the release once it lands
        # Only after auto-merge actually completed: a tag on a commit that
        # never reached main points at nothing. A PAT-pushed tag triggers
        # release.yml, which a GITHUB_TOKEN-pushed one would not.
        if: steps.reported.outputs.count != '0' && env.changed == 'true' && env.tag != ''
        env:
          GH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
        run: |
          for _ in $(seq 60); do
            state=$(gh pr view "chore/tool-refresh-$(date -u +%Y%m%d)" --json state -q .state || echo UNKNOWN)
            [ "$state" = "MERGED" ] && break
            sleep 60
          done
          if [ "$state" != "MERGED" ]; then
            echo "::warning::the refresh PR has not merged yet; tag $tag by hand once it does"
            exit 0
          fi
          git fetch origin main
          git tag "$tag" origin/main
          git push origin "$tag"
